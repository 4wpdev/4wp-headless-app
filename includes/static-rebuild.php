<?php
/**
 * Static-export rebuild disabled — site uses live SSR from WP API.
 * Kept as a stub so older deploys do not fatally require the file.
 */
defined( 'ABSPATH' ) || exit;

// No-op: content changes appear on next page request (Next.js force-dynamic).
